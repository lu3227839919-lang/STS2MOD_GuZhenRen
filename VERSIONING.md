# 版本管理规范（VERSIONING）

本项目为《杀戮尖塔 2》Mod「蛊真人」（GuZhenRen）。本文件定义版本号、分支、提交、标签与发布流程的统一约定。所有协作者（包括未来的自己）都应遵守。

---

## 1. 版本号规则

采用 **SemVer 风格**：`主版本.次版本.补丁`（如 `0.6.6`）。当前处于内容快速迭代期，主版本保持 `0`，代表**不承诺向后兼容**。

| 段位 | 何时递增 | 示例 |
|---|---|---|
| 主版本（x） | 里程碑式重构 / 不兼容的大改版 | `0.6.6` → `1.0.0` |
| 次版本（y） | 新增卡牌、新机制、新系统、玩法变更 | `0.6.5` → `0.6.6` |
| 补丁（z） | Bug 修复、文案/图标/平衡微调、兼容性修复 | `0.6.6` → `0.6.7` |

> 游戏本体也在更新（本项目跟随 `sts2` API 适配），Mod 版本号与游戏版本号**各自独立**，互不绑定。

## 2. 分支模型

| 分支 | 职责 | 规则 |
|---|---|---|
| `dev` | 日常开发 | 所有功能、修复直接提交到 `dev` |
| `main` | 发布分支 | 仅存放已发布/可发布的稳定版本 |

**发布流程（dev → main）：**
1. 在 `dev` 完成开发与验证（`dotnet build` 通过）；
2. 合并到 `main`：`git checkout main && git merge --ff-only dev`（优先 fast-forward，保持线性历史）；
3. 在 `main` 打 tag（见第 4 节），推送 `main` 与 tag；
4. 切回 `dev` 继续开发。

> 除非 main 已偏离（线上热修），否则不创建 merge commit，保持历史线性。

## 3. 提交规范（Commit Message）

格式：**`类型: 中文描述`**（类型小写 + 半角冒号 + 空格）。描述用简体中文，动词开头、具体说明「改了什么 / 为什么」。

| 类型 | 用途 |
|---|---|
| `feat` | 新功能、新卡牌、新机制 |
| `fix` | Bug 修复 |
| `refactor` | 重构，不改变行为 |
| `perf` | 性能优化 |
| `docs` | 文档、注释、README、本规范 |
| `style` | 格式、命名、排版，不影响逻辑 |
| `chore` | 构建、工具、依赖、杂务 |

示例：
```
feat: 血道蛊虫补全一至九转
fix: 修复篝火为蛊虫升转无法升转两只的 bug
refactor: 蛊虫升级接口改为五转升六转
docs: 更新 README 版本信息
```

避免使用「暂时同步」这类无信息量的信息；如有阶段性同步需求，也请附上类型与内容摘要。

## 4. 标签（Tag）规范

- 格式统一为 **小写 `v` + 完整版本号**：`v0.6.6`、`v1.0.0`。
- **每次发布必打 tag**，打在 `main` 的发布提交上：
  ```
  git tag -a v0.6.6 -m "0.6.6 版本发布"
  git push origin v0.6.6
  ```
- 历史遗留的大小写混杂 tag（`v0.3.1`、`V0.4.14` 等）**保持原样不再改动**，仅影响追溯，不影响新规范。

## 5. 变更记录

| 文件 | 内容 | 维护时机 |
|---|---|---|
| `CHANGELOG.md`（仓库根目录） | 全部版本的正式变更日志，按版本降序 | 每次发布追加 |
| `update_note/PATCH_NOTES_x.y.z_简短主题.md` | 当次发布的详细说明（开发记录/验证说明） | 每次发布新建 |

CHANGELOG 条目结构：
```
## [0.6.6] - 2026-08-06
### 新增
- ...
### 修复
- ...
### 调整
- ...
```

## 6. 发布检查清单（Release Checklist）

发布 `x.y.z` 前逐项确认：

1. [ ] `dotnet build GuZhenRen.csproj` 编译通过（0 错误）
2. [ ] 三处版本号一致：`GuZhenRen.json` 的 `version`、`README.md` 的「Mod 版本」、即将打的 tag
3. [ ] `update_note/PATCH_NOTES_x.y.z_*.md` 已编写
4. [ ] `CHANGELOG.md` 已追加 `[x.y.z]` 条目
5. [ ] 合并 `dev` → `main`（fast-forward）并推送
6. [ ] 在 `main` 打 tag `vx.y.z` 并推送
7. [ ] 生成/更新发布物（.pck、.dll、zip 压缩包）并上传发布渠道

## 7. 隐私与安全约定

- `local.props`（本机路径）**永不提交**（已在 .gitignore）；
- 本机绝对路径、会话/命令历史（如 `reasonix.toml`）不得进入仓库，一旦误提交应尽快 `git rm --cached` 解除跟踪。
