# Changelog

本项目的全部重要变更记录。版本号遵循 [SemVer](https://semver.org/lang/zh-CN/) 风格，规则见 [VERSIONING.md](VERSIONING.md)。历史版本大多未标注发布日期。

## [0.6.6]

- 血颅蛊新增「噬骸 X」关键词，X 表示本次至多可选择并吸收的遗骸数量。
- 原隐藏增伤状态改为可见的「闪耀 X」Power，X 表示下一张光道攻击第一段提高的伤害。
- 蛊存放牌堆上限改为 5 张、永久蛊虫容量改为 15 张；多张已恢复蛊竞争空位时使用多人同步的玩家独立随机流抽取。
- 超出上限的已恢复蛊留在蛊恢复堆待命；杀招推演仍只允许使用蛊存放牌堆中的可用蛊。

## [0.6.5]

- 保留血颅蛊、血颅 Power 与全部联动数值，仅删除重复的「血颅」自定义关键词及已由卡牌正文或可见 Power 说明的仙元、折光、照破、聚光、定光、留光、破镜、血元、血寄、遗骸、流血、血印、血河等重复悬浮关键词。
- 旧存档与多人同步快照中残留的上述关键词 ID 会在读取时清理。
- 催动关键词精简为「蛊牌必须借助手牌中的‘催动’使用」；真正蛊牌正文统一以“{Rank}转”（英文 “Rank {Rank}”）开头，使用现有动态变量、不重新注册转数关键词。

## [0.6.4]

- 移除转数、蛊虫、光辉、元气四类冗余悬浮关键词的注册与卡牌展示，旧存档或多人同步快照残留标签在读取时清理，不改变卡牌数值或资源机制。
- 无参数「耀化」改为「耀化 1」至「耀化 9」：达到对应光辉时自动支付并触发强化，不足时不支付也不强化；月光蛊与五转以上月芒蛊使用「耀化 2」。
- 删除卡牌正文中重复手写的“消耗 / Exhaust”，保留原生 `CardKeyword.Exhaust` 与唯一一份悬浮说明。

## [0.6.3]

- 杀招配方改为无序多重集合匹配：材料种类与数量相同即可，选择顺序不再影响结果；推演失败/资源不足/创建失败/联机状态变化时显示原因，不扣能量/元气、不移材料，配方材料快照使用稳定排序保证多人端一致。
- 修复低语空谷「拥抱树木」转换无候选时抛异常并卡死事件：方源基础牌在基础池转换、蛊虫在蛊池转换；满蛊虫容量时忽略被替换的原牌，最终无合法候选则安全保留原牌。
- 遗骸消耗改为同步网格选择、只展示手牌中的遗骸，未通过完整校验不消耗任何牌；修复刀翅血蝠蛊选项预创建两张正式战斗牌造成的不可见孤儿牌。
- 带道途状态的卡牌显示「流派-卡名」（如“光道-小光蛊”）；精简血道卡牌正文，并在所有卡牌界面开放相关自定义关键词说明。
- 整合血寄胎变完整链路：血气蛊补全一至九转并支持高转跨牌堆多次触发，血月蛊改为光血合练蛊，血胎吞寄与 3 点胎动孵化，破胎结算保持 Replay 整段只推进一次寄生；保留血元、遗骸、血颅、血蝠与三张血道杀招的既有联动。

## [0.6.2]

- 自定义关键词说明仅在牌堆/永久牌组查看界面显示，卡面描述继续使用 `ModKeywordCardDescriptionPlacement.None`，游戏本体消耗、保留提示不受过滤。
- 仙元牌改为由当前 `CombatState.CreateCard` 创建，修复余额归零时无法进入消耗堆的问题。
- 奖励满容量检查不再读取 canonical 候选牌的 `Owner`；`GetPossibleCards` 的蛊虫奖励过滤只在真正的卡牌奖励生成中启用，不影响攻击药水等临时生成。

## [0.6.1]

- 适配游戏 0.110.1：`CardCmd.Exhaust` 参数签名变更导致初始化 `MissingMethodException` 回滚，改为动态枚举现有重载、只挂载返回 `Task` 且含 `CardModel` 参数的方法，找不到时记录警告并跳过，不再阻止模组初始化。
- 模组内部所有主动消耗调用统一走 `CardExhaustCompat.ExhaustAsync`，避免运行相关卡牌效果时再次触发缺失方法。
- 全部蛊虫统一关键词提示：蛊虫、当前转数、催动、恢复、元气，六转以上额外显示仙蛊、仙元，并按机制显示光辉、折光、照破、血元、遗骸等术语；关键词不自动插入卡面正文，只在悬浮提示框显示。

## [0.6.0]

- 血气蛊补全一至九转，按转数拥有 1 至 3 次持续触发；血月蛊改为「月光蛊＋血气蛊」合练蛊（最低四转），血月寄生按残月→盈月→满月推进，覆盖血气时从盈月开始。
- 血胎吞寄与胎动：可吞噬血气或未满月的月相并继承 1 至 2 点胎动，3 点胎动孵化；未成熟寄生宿主结算后进入消耗堆时破胎（获得 1 血元并清除寄生）；Replay 完整序列仍只推进一次寄生。
- 使用 RitsuLib `RegisterOwnedCardKeyword` 注册催动、恢复、合练、血元、血寄、血气、月相、血胎、胎动、破胎、孵化、吞寄、遗骸、血颅等机制词并悬浮提示，不自动插入卡面正文。
- 催动机制：催动不能再被玩家手动打出，手牌中有可支付费用的催动时蛊牌可直接使用并自动代打（多张时优先费用最低者）；仙元归零不再立即 Exhaust，登记为待消耗并在蛊牌最后一段 CardPlay（含 Replay）完成后统一移入消耗堆，修复仙元耗尽时蛊牌悬挂不结算问题。
- 存档兼容与破胎时序：旧版残月寄生枚举运行时映射为新版月相，新增阶段与触发次数用 `SavedAttachedState` 保存；新增 `XueDaoParasiteExhaustPatch` 包装 `CardCmd.Exhaust` 处理破胎时序。

## [0.5.0]

- 新增完整血道体系：血道寄生（宿主最后一段 CardPlay 后触发一次并清除）、遗骸（0 费保留消耗牌、主动使用获得 2 点血元）、血颅（最多 3 层 Buff，强化寄生/防御/血蝠多段）。
- 新增血气蛊、血月蛊、血池蛊、血皮蛊、血颅蛊、刀翅血蝠蛊六只普通血道蛊，以及血胎蛊、血蝠王蛊、血河甲蛊三只合练蛊（结果转数为材料最低转数＋1，最高九转）。
- 新增血漂流、万骸归潮、血月祭三个血道杀招。
- 资源与安全边界：血元上限 12、固定费用免确认、血月蛊不允许生命代付；遗骸每主要敌人最多 1 张、每次 CardPlay 最多 2 张；群体伤害/流血/血印按全场总额度分配，血蝠击杀不产生遗骸。
- 新增 15 个控制台卡牌 ID；本次源码未附带新卡图，缺图时回退到基础资源并输出卡图审计警告。

## [0.4.17]

- 升炼多选与手动确认：每次升炼可选 0 至 2 只符合条件的蛊虫，不再要求牌组先拥有六转蛊虫；必须主动点击确认提交，选择 0 只确认等同取消、不消耗休息点升炼机会。
- 蛊虫升级规则重构：蛊牌完全不可使用游戏原生卡牌升级（锻造与升级事件只允许普通牌），包括五转升六转在内的所有转数提升均由升炼完成；每次恢复后最大催动次数固定为 1。
- 升级状态映射：一至五转蛊牌 `IsUpgraded=false`，六至九转派生为 `true`（不执行原生升级生命周期、不获得任何旧版升级收益）；旧存档残留的升级标记被该映射覆盖。
- 升炼数量分阶段：凡蛊最多升炼 2 只、仙蛊只能升炼 1 只，先选第一只后按凡/仙分流（凡蛊再提供一次可选选择，仙蛊直接单只结算）。
- 兼容处理：战斗恢复转数不使用派生的 `IsUpgraded` 作为匹配键，卡牌引用预览不对真正蛊虫调用 `CardCmd.Upgrade`；列出 5 张缺失卡图（GuangBiao、CanMang、NingJingHui、YuGuangYi、ZheGuang）。

## [0.4.16]

- Dusty Tome 不再固定给予镜月返照：进入 DARV 时从普通合法蛊虫池确定性随机选择一张，优先排除全队已有同名仙蛊的候选，领取后初始化为六转并保留原版升级效果。
- 杀招推演入口改为右键点击「蛊恢复堆」（右键蛊手牌不再触发），推演材料仍从当前可用蛊手牌中选择，费用/配方/反噬/多人同步规则不变。

## [0.4.15]

- DARV/Dusty Tome 修复：方源进入 DARV 时直接将其 AncientCard 设置为镜月返照，领取遗物前再次校验卡牌 ID，兼容事件恢复、QuickSL 与其他 Mod 的补丁顺序。
- Dusty Tome 成功领取后将镜月返照初始化为九转，原版仍负责升级；其他角色继续使用原版或 BaseLib 逻辑。

## [0.4.14]

- 先古遗民兼容：为 `DustyTome.SetupForPlayer(Player)` 添加仅针对蛊真人角色的 Harmony finalizer，只吞掉 `NullReferenceException`，兼容 BaseLib `ITomeCard` 扩展在自定义角色卡池上的空引用。
- 补齐 DARV、OROBAS、PAEL、TANX、TEZCATARA、VAKUU、NONUPEIPE 七位先古遗民的中英文对话。
- 源码发布清理：删除卡图诊断中的本地路径输出，发布包不再包含 `local.props`，新增 `tools/Build-SourceArchive.ps1` 自动排除本机配置、构建目录、日志与补丁文件。

## [0.4.13]

- 修复高转蛊虫进入战斗后回退一转：转数同时写入普通实例字段与 RitsuLib `SavedAttachedState`，分别负责克隆与存档/读档/多人快照。
- `Player.PopulateCombatState` 在战斗牌组创建前记录永久牌组转数，战斗实例生成后按卡牌 ID、升级状态和重复顺序二次校准；成功校准时输出 `[蛊虫转数]` 日志。
- 修复恢复衍生牌触发：三转蛊虫耗尽时衍生牌进弃牌堆，四转以上在下一恢复回合进手牌（手牌满进弃牌堆）；普通升级后仍有剩余催动次数的蛊虫不提前生成。
- 战斗初始化不再按战斗牌堆顺序给同名蛊虫重新分配转数，回退修复按卡牌 ID、普通升级状态和附魔 ID 分组，避免华彩附魔实例与同名一转/三转/五转蛊虫互换转数。

## [0.4.12]

- 原生 `card` 控制台指令支持指定蛊真人卡牌转数（`card <ID> <转数>` 或 `card <ID> rank=N`），给予时不再需要指定牌堆。
- 战斗中蛊虫牌自动进入蛊恢复堆并从当前回合开始计算恢复，其他牌进入普通手牌；非战斗场景所有卡牌直接进入永久牌组，附带原生位置参数也会按规则校正。
- 非法转数自动限制到卡牌自身允许范围；控制台补丁为可选开发功能，未来游戏版本移除对应原生方法时模组主体仍可初始化。

## [0.4.3]

- 新增流光蛊（LiuGuangGu）及流光、流辉、白虹三张战斗衍生牌。
- 新增白虹贯日、镜月返照两个杀招及有序配方（月光蛊↔流光蛊、月光蛊↔镜光蛊）。
- 全部新牌使用同名卡图，缺失时继续由 `CardImageCatalog` 输出警告。

## [0.4.2]

- 所有具体卡牌统一使用与 C# 运行时类型同名的 PNG（`res://GuZhenRen/images/{CardTypeName}.png`）。
- 新增 `CardImageCatalog`：初始化时扫描程序集内所有具体 CardModel，缺图逐张输出 `[卡图缺失]` 警告并显示 Godot 路径，不再静默复用其他卡牌图片。
- 更新《蛊虫卡牌大全》图片路径索引，新增 `CARD_IMAGE_NAMES.txt` 与卡图审计文档。
- 上传源码包未携带 PNG，静态审计识别到的 45 张具体卡牌均标记为缺图，本地放入同名 PNG 后运行时按实际资源状态输出。

## [0.4.1]

- 新增镜光蛊、定光蛊及合练蛊「镜辉蛊」（三转以上镜光蛊＋定光蛊），恢复期间生成光镜/定光符体系普通牌。
- 月芒蛊重构：配方仍为月光蛊×1＋小光蛊×2（均需三转），结果转数改为材料最低转数，五转起可支付光辉增加攻击段数并施加照破，衍生牌链为月芒/凝月芒/残芒/天月芒。
- 新增配方最低转数检查：低于三转的材料蛊虫不再出现在对应合练材料选择中。
- 新卡牌全部经 RitsuLib `RegisterCard` 自动注册，恢复阶段状态用 `SavedAttachedState` 保存，元气使用次级资源接口、自定义牌堆管理蛊存放/恢复堆。
- 未移除月芒蛊稳定模型类型，旧存档中的月芒蛊仍可解析，数值与机制按 0.4.1 代码重新计算。

## [0.4.0]

- 蛊虫永久牌组容量从 10 提升至 15；满容量时新蛊奖励仍正常出现，选中后必须替换一张合法旧蛊，替换流程事务安全（先加入成功再移除旧牌）。
- 蛊虫恢复时间不再全体固定为「耗尽回合 + 2」，改为由具体蛊虫和转数决定；恢复牌堆成为机制阶段，三转以上蛊虫可在进入恢复堆或下一回合生成衍生牌。
- 小光蛊成长重构：一至五转每回合第一只获光辉、后续强化指定光道攻击；二转聚光、三转微光、六转聚光、八转余辉、九转极光，恢复时间随转数从 1 回合提升至 3 回合。
- 月光蛊成长重构：三转月刃、四转恢复期入牌、五转耀化、七转残月、九转满月刃；Replay 不重复生成牌或施加照破。
- 玉皮蛊删除永久敏捷与玄玉掌生成，回归纯防御反光定位（玉膜/玉光衣/折光/琉璃玉衣）；新增光道防御杀招「月霓裳」（月光蛊↔玉皮蛊有序配方），不占蛊虫容量。

## [0.3.3]

- 玉皮蛊：每张牌每场战斗仅第一次催动获得敏捷，敏捷表调整为 0/0/1/1/1/1/2/2/3，六至九转格挡调整为 6/8/10/12。
- 玄玉掌：六至九转伤害/格挡调整为 14/15、16/17、18/19、20/21；七转起降 1 费，八九转 Replay 1 且 Replay 只重复伤害、格挡仅首段结算。
- 月光蛊六至九转伤害为 11/12/13/14 且 Replay 不再次施加照破；小光蛊六至九转虚弱/易伤为 3/3/4/4、追加照破为 1/1/2/2。
- 血印改为全场总额度按确定性敌人顺序尽量均分，不再向每个敌人复制完整层数；七转 Replay 只重复伤害，血印返还改为每个 CardPlay 全场最多 1 点血元。
- 升炼默认每次 1 张、牌组存在六转蛊虫后可选 1 至 2 张；满 10 张时新蛊仍可入奖励/商店，获得后必须替换一张合法旧蛊，替换失败不提前移除旧蛊。

## [0.3.2]

- 修复血月蛊配方不可完成：合练材料不再一刀切排除所有合练结果牌，月芒蛊注册为材料后可与血气蛊合练出血月蛊。
- 修复五转玉皮蛊生成六转玄玉掌时同时降费和 Replay 的问题：六转仅获数值成长，七转起降 1 费、八转获得 Replay 1、九转总计降 2 费并 Replay 2。
- 修复血气蛊三至五转可重复净治疗：失去生命与下回合治疗量统一为同一数值（2/3/4），不再产生净生命收益；六转小光蛊照破改为追加固定层数，不再按已有层数翻倍。
- 统一 Replay 支付规则（每次出牌序列只扣一次元气/仙元/催动次数）；蛊虫恢复改为逐牌两回合冷却；催动升级移除虚无；月芒蛊最大催动次数修正为 2。
- 蛊虫永久牌组容量上限 10 张并执行容量检查；光辉支付改为玩家选择；多目标战斗中催动攻击蛊由玩家选敌；仙道最大生命成长下调（总计 +10）；合并蛊虫卡池并修复空候选奖励的 `ArgumentOutOfRangeException`。

---

> 说明：0.3.1 及更早版本无补丁说明文件；0.4.4~0.4.11 无补丁说明文件（未收录）。各版本发布日期均未标注，后续版本请在条目标题中补充日期（如 `## [0.6.7] - 2026-08-07`）。
