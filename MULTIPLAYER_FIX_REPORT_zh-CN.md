# 蛊真人多人模式修复与审计报告

## 结论

本次 checksum #17 的根因不是随机数，也不是双方模组版本不同，而是
RitsuLib ExtraHand 的本地预移动与多人动作执行发生重叠：

- 主机在“小光蛊”仍在执行时选择了下一张“月光蛊”；
- ExtraHand 立即只在主机本地把“月光蛊”移入原版 Hand；
- 客机只收到下一动作入队消息，尚未执行该动作，因此“月光蛊”仍在蛊牌堆；
- “小光蛊”结束校验时，主机 Hand 有 3 张、客机 Hand 有 2 张，
  多出的正是“月光蛊”。

报告中的全部跑局 RNG 状态、玩家 RNG 状态、SavedProperty 映射和双方
GuZhenRen/RitsuLib 版本均一致。远端额外启用的 SavesMerger 被标为
non-gameplay，不符合这次具体差异的时序和字段。

## 已修复

1. 跟踪 PlayCardAction 的完整异步执行周期；任一卡牌动作正在执行时，
   Gu ExtraHand 不再开始下一次本地选择，避免把下一张蛊牌提前移入 Hand。
2. 一个 pending 蛊牌选择未清理前，禁止另一张蛊牌覆盖它及其预留“催动”。
3. 相同费用的多张“催动”增加网络卡牌 ID 终结排序键，确保各端消耗
   同一个战斗卡实例。
4. 同名、同转、同升级蛊牌随机入场增加网络卡牌 ID 终结排序键。
5. 仙元支付和批量消耗、战后遗骸保留、杀招材料稳定排序均增加网络
   卡牌 ID 终结排序键，消除相同值卡牌继承本地集合/牌堆枚举顺序的风险。

## 其他日志与审计结果

- godot(1).log 中大量 Unknown card/character ID 出现在 GuZhenRen 被设置为
  disabled 的启动流程，属于禁用内容后读取旧进度的警告，不是这次联机分歧。
- 退出联机大厅后的 Godot Variant System.Object 异常没有进入 GuZhenRen
  调用栈；现有日志不足以归因到本模组，因此没有做猜测性修改。
- 小光蛊“每回合首次获得光辉”的 TurnGainStates、部分视觉/动画协调状态
  使用 ConditionalWeakTable。它们在正常锁步动作中会得到相同调用顺序，
  但“中途加入或从战斗快照恢复”的覆盖仍建议做双端实机回归。

## 建议回归场景

1. 两名玩家轮流快速点击蛊牌，尤其在上一张蛊牌动画尚未结束时点击下一张。
2. 同一手牌存在两张同费用催动，连续催动两张蛊牌。
3. 同名、同转蛊牌超过五张，验证开场与恢复随机入场。
4. 同时消耗多张同类型仙元，验证 Hand/Exhaust 顺序。
5. 杀招选择同名同转材料、战后保留多个遗骸。
6. 主客互换，并在高延迟环境下重复上述场景。

## 验证说明

- 已对所有修改的 C# 文件执行语法树解析；
- 已核对补丁标记、差异文件清单、ZIP 完整性与 SHA-256；
- 当前环境没有目标游戏的 sts2.dll、Godot/.NET SDK，因此无法在此处完成
  实际项目编译或双端联机运行；发布前仍需在 0.110.1 + RitsuLib 0.5.10
  环境完成上述实机回归。
